# Copy this file to `.env` and adjust the values.
# Consumed by scripts/set-env.mjs to generate src/environments/environment.ts
# at start/build time (npm start, npm run build, npm run watch, npm test).

# Base URL of the backend API.
APP_URL=http://localhost:3000
